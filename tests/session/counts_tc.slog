;; Count-round fixture (docs/incremental.md §8B, M0): transitive closure
;; over a tiny graph with one redundantly-derivable edge.  Hand-verified
;; sidecar contents after (recount):
;;   edge rows              -> input=0 (1,0)      (program facts, §8B.5)
;;   path (1 2), (2 3)      -> (nonrec 1, rec 0)  (copy rule only)
;;   path (1 3)             -> (nonrec 1, rec 1)  (copy rule + edge(1,2)*path(2,3))

table (edge int int)
table (path int int)

rule
(edge 1 2)
(edge 2 3)
(edge 1 3)

rule
(edge X Y)
-->
(path X Y)

rule
(edge X Y)
(path Y Z)
-->
(path X Z)
